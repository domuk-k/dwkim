{
  "name": "dwkim-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "dwkim workspace",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm --filter dwkim build",
    "dev": "pnpm --filter dwkim dev",
    "lint": "pnpm --filter dwkim lint",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.4"
  }
}
